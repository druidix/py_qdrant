[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "qdrant-lib"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "qdrant-client",
    "python-dotenv",
]

[tool.setuptools]
packages = ["qdrant_lib"]
